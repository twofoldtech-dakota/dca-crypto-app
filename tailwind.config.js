module.exports = {
    purge: [],
    darkMode: "class", // or 'media' or 'class', false by default
    theme: {
        extend: {},
    },
    variants: {
        extend: {},
    },
    plugins: [],
};
